import React from "react";
import { Referral } from "../../components";

const ReferralPage = () => {
  window.scrollTo(0, 0);
  return (
    <div>
      <Referral />
    </div>
  );
};

export default ReferralPage;
